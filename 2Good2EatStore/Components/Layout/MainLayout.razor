@using _2Good2EatStore.Data.Interfaces
@inherits LayoutComponentBase
@inject IProductService productService;
@inject ICartService cartService;
<MudThemeProvider Theme="MuudstrapTheme" IsDarkMode="true" @rendermode="InteractiveServer" />
<MudPopoverProvider />
<MudDialogProvider />
<MudSnackbarProvider />

<MudLayout>
    <MudAppBar Elevation="5" Gutters="false" Class="muudstrap-navbar">
        <NavMenu />
    </MudAppBar>
    <MudMainContent Class="mt-16 pa-4">
        @Body
    </MudMainContent>
</MudLayout>

@code {

    MudTheme MuudstrapTheme = new MudTheme()
        {
            Typography = new Typography()
            {
                H6 = new H6()
                {
                    FontFamily = new[] { "system-ui", "-apple-system", "Segoe UI", "Roboto ", "Helvetica Neue", "Arial", "Noto Sans", "Liberation Sans", "sans-serif", "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji" },
                    FontSize = "1.25rem",
                    FontWeight = 400,
                    LineHeight = 1.7,
                    LetterSpacing = "normal"
                },
                Button = new MudBlazor.Button()
                {
                    FontFamily = new[] { "system-ui", "-apple-system", "Segoe UI", "Roboto ", "Helvetica Neue", "Arial", "Noto Sans", "Liberation Sans", "sans-serif", "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji" },
                    FontSize = "1rem",
                    FontWeight = 400,
                    LineHeight = 1.5,
                    LetterSpacing = "normal"
                },
            }
        };


}